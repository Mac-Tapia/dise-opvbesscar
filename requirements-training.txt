# Requirements para entrenamiento RL (A2C, PPO, SAC) - PYTHON 3.11 REQUERIDO
# =============================================================================
#
# Este archivo contiene las dependencias ADICIONALES necesarias para entrenar
# agentes RL con GPU (si está disponible).
#
# Instalación:
#   python -m venv .venv
#   .venv\Scripts\activate (Windows) o source .venv/bin/activate (Linux)
#   pip install -r requirements.txt
#   pip install -r requirements-training.txt
#
# ⚠️  SOLO PYTHON 3.11 - NO SOPORTA 3.12, 3.13, etc.
# ⚠️  Requiere requirements.txt instalado primero

# ============================================================================
# RL TRAINING SPECIFIC
# ============================================================================
# Callbacks, utilities para stable-baselines3 training
sb3_contrib==2.7.1

# ============================================================================
# MONITORING & LOGGING (para tracking de training)
# ============================================================================
tensorboard==2.20.0
tensorboard_data_server==0.7.2
wandb==0.24.0

# ============================================================================
# NOTE: Todos los requirements de base están en requirements.txt
# Este archivo solo contiene las ADICIONALES para training GPU.
# Install order:
#   1. pip install -r requirements.txt
#   2. pip install -r requirements-training.txt
# ============================================================================

