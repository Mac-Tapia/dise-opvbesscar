oe1:
  grid_connection:
    available_capacity_kva: null
    continuity: sistema aislado termico
    power_factor: 0.95
  site:
    access_notes: Acceso vial y seguridad verificados en visita de campo.
    area_estacionamiento_m2: 957.0
    area_per_moto_m2: 2.0
    area_per_mototaxi_m2: 4.0
    area_techada_m2: 20637.0
    coverage_required_pct: 0.65
    distance_to_substation_m: 60.0
    dwell_hours_min: 4.0
    name: Mall de Iquitos
    restrictions_notes: Sombras puntuales por equipos; sin arbolado alto.
    security_notes: Zona de estacionamiento con vigilancia.
    vehicles_peak_motos: 900
    vehicles_peak_mototaxis: 130
oe2:
  bess:
    autonomy_hours: 4.0
    c_rate: 0.6
    discharge_only_ev_hours: true
    discharge_only_no_solar: true
    dod: 0.8
    efficiency_roundtrip: 0.9
    fixed_capacity_kwh: 4520.0
    fixed_power_kw: 2712.0
    load_scope: ev_only
    min_soc_percent: 25.86
    pv_night_threshold_kwh: 0.1
    round_kwh: 100
    sizing_mode: open_hours
    surplus_target_kwh_day: 9630.4
  dispatch_rules:
    description:
      "Cubrir bloque pico (18-21h) con prioridades: FV\xE2\u2020\u2019\
      EV \xE2\u2020\u2019 FV\xE2\u2020\u2019BESS \xE2\u2020\u2019 BESS\xE2\u2020\u2019\
      EV(noche) \xE2\u2020\u2019 BESS\xE2\u2020\u2019MALL(saturada)"
    enabled: true
    priority_1_pv_to_ev:
      description: "FV directamente a EVs (m\xC3\xA1xima prioridad)"
      enabled: true
      ev_power_limit_kw: 150.0
      pv_threshold_kwh: 0.5
    priority_2_pv_to_bess:
      bess_power_max_kw: 2712.0
      bess_soc_max_percent: 95.0
      bess_soc_target_percent: 85.0
      description: FV excedente a BESS (cargar reserva para pico)
      enabled: true
    priority_3_bess_to_ev:
      bess_power_max_kw: 2712.0
      bess_soc_min_percent: 20.0
      description: BESS a EVs cuando cae el sol (noche)
      enabled: true
      pv_night_threshold_kwh: 0.1
    priority_4_bess_to_mall:
      bess_saturation_percent: 95.0
      description:
        "BESS saturada + FV excedente \xE2\u2020\u2019 Mall (descargar\
        \ exceso)"
      enabled: true
      mall_power_max_kw: 500.0
    priority_5_grid_import:
      allowed: true
      cost_multiplier_peak: 2.0
      description: "Importar desde red si es necesario cubrir d\xC3\xA9ficit"
      enabled: true
    reward_bonuses:
      battery_charge_bonus_weight: 0.005
      direct_solar_bonus_weight: 0.01
      grid_import_penalty_weight: 0.0001
  ev_fleet:
    # Configuración de cargadores (DATOS REALES OE2 2026-01-30)
    charger_power_kw_moto: 2.0          # 28 cargadores motos @ 2kW cada uno
    charger_power_kw_mototaxi: 3.0      # 4 cargadores mototaxis @ 3kW cada uno
    total_chargers: 32                  # Total físico: 28 + 4 = 32 cargadores
    total_sockets: 128                  # Total sockets: 32 × 4 = 128 (112 motos + 16 mototaxis)
    # Demanda EV constante para tracking CO₂ (workaround CityLearn 2.5.0 bug)
    ev_demand_constant_kw: 50.0         # Estimación conservadora (54% uptime × 100kW = ~50kW)
    closing_hour: 22
    daily_sessions_factor: 1.0
    fc_motos: 0.9
    fc_mototaxis: 0.9
    motos_count: 900
    mototaxis_count: 130
    opening_hour: 9
    pe_motos: 0.9
    pe_mototaxis: 0.9
    peak_hours:
      - 18
      - 19
      - 20
      - 21
    peak_share_day: 0.5
    session_minutes: 30
    sockets_per_charger: 4
    utilization: 0.92
  location:
    alt: 104.0
    lat: -3.75
    lon: -73.25
    tz: America/Lima
  mall:
    energy_kwh_day: 9202.4
    shape_24h: null
  operational_control:
    bess_soc_target:
      during_peak_hours: 0.4
      normal_hours: 0.6
      pre_peak_hours: 0.85
    fairness_penalty_weight: 0.15
    import_penalty_weight: 0.3
    peak_cost_multiplier: 1.5
    peak_hours:
      - 18
      - 19
      - 20
      - 21
    power_limits_kw:
      playa_motos: 120.0
      playa_mototaxis: 48.0
      total_aggregate: 150.0
    soc_reserve_penalty: 0.2
    valley_hours:
      - 9
      - 10
      - 11
      - 12
  solar:
    area_total_m2: 20637.0
    candidate_count: 5
    factor_diseno: 0.65
    inverter_name: Eaton__Xpert1670
    module_name: Kyocera_Solar_KS20__2008__E__
    scale_to_target_annual: true
    selection_metric: energy_per_m2
    selection_mode: auto_top5
    surface_azimuth: 0.0
    surface_tilt: 10.0
    target_ac_kw: 3201.2
    target_annual_kwh: 8030119
    target_dc_kw: 4162.0
    use_pvlib: true
oe3:
  baseline_episodes: 3
  city_baseline_tpy:
    electricity_generation: 290000.0
    transport: 258250.0
  co2_emissions:
    annual_co2_budget_kg: 7000000
    bess_calendar_aging_kg_per_day: 0.01
    bess_charging_efficiency: 0.95
    bess_cycling_co2_per_cycle: 0.05
    bess_discharging_efficiency: 0.95
    bess_standby_loss_percent_day: 0.1
    diesel_backup_factor_kg_kwh: 0.2667
    grid_import_factor_kg_kwh: 0.4521  # Factor de emisión grid Iquitos (central térmica aislada)
    # TRACKING CO₂ DIRECTO (workaround CityLearn 2.5.0):
    # - ev_demand_kw = 50 kW constante (definido en agentes SAC/PPO/A2C)
    # - co2_direct_kg = 50 kW × 2.146 kg/kWh = 107.3 kg/h acumulativo
    # - motos_activas = (50 × 0.80) / 2 = 20 motos/h
    # - mototaxis_activas = (50 × 0.20) / 3 = 3 mototaxis/h
    reduction_strategies:
      bess_efficiency_optimization:
        description: Optimizar ciclos y conversiones BESS
        enabled: true
        max_cycles_per_year: 200
        weight: 0.15
      cost_reduction:
        description: Minimizar costo operativo (colateral)
        enabled: true
        weight: 0.05
      direct_solar_maximization:
        co2_avoided_per_kwh: 0.4521
        description: Maximizar FV directo a EVs (P1 despacho)
        enabled: true
        weight: 0.5
      grid_import_minimization:
        description: "Minimizar importaci\xC3\xB3n desde grid"
        enabled: true
        penalty_multiplier_peak_hours: 2.0
        weight: 0.3
  dataset:
    central_agent: true
    name: iquitos_ev_mall
    template_name: citylearn_challenge_2022_phase_all_plus_evs
  emissions:
    # CO₂ evitado por vehículos eléctricos vs combustión
    kgco2_per_gallon: 8.9      # kg CO₂ por galón de gasolina (combustión)
    km_per_gallon: 120.0        # Eficiencia motos/mototaxis a combustión (km/galón)
    km_per_kwh: 35.0            # Eficiencia motos/mototaxis eléctricos (km/kWh)
    project_life_years: 20
  evaluation:
    a2c:
      batch_size: 146 # ✓ FIXED: 64→146 (perfect divisor: 8760/146=60 mini-batches, no truncation)
      checkpoint_freq_steps: 200
      device: cpu # Changed from cuda to avoid PyTorch Dynamo issues
      entropy_coef: 0.001
      episodes: 3
      gamma: 0.99 # Paper standard
      gae_lambda: 0.95 # Paper: GAE
      learning_rate: 0.0001 # 1e-4 (can match PPO, on-policy simple)
      learning_rate_schedule: linear
      log_interval: 100
      max_grad_norm: 0.5
      n_steps: 128 # 128 (vs Paper variable), short rollouts
      normalize_advantage: true # Paper standard
      normalize_observations: true
      normalize_rewards: true
      reward_scale: 0.1
      hidden_sizes: [128, 128]
      use_rms_prop: true # Paper: RMSProp is standard for A3C/A2C
      progress_interval_episodes: 1
      save_final: true
      multi_objective_weights:
        co2: 0.35   # BALANCED: Prioridad principal
        cost: 0.25  # Optimización de costos
        ev: 0.15    # Satisfacción de EVs
        grid: 0.05  # Estabilidad de red
        solar: 0.20 # Autoconsumo solar
    agents:
      - SAC
      - PPO
      - A2C
    co2_tracking: true
    multi_objective_priority: balanced
    ppo:
      batch_size: 120 # ✓ FIXED: 256→120 (perfect divisor: 8760/120=73 mini-batches, no truncation)
      checkpoint_freq_steps: 500
      device: cuda
      episodes: 3
      ent_coef: 0.01 # ↑ OPTIMIZED: 0.001→0.01 (exploration incentive)
      gamma: 0.99 # Paper standard
      gae_lambda: 0.98 # ↑ OPTIMIZED: 0.95→0.98 (better long-term advantages)
      kl_adaptive: true # ↑ OPTIMIZED: Enable adaptive KL divergence
      target_kl: 0.02 # ↑ OPTIMIZED: NEW - Early stopping for KL divergence
      learning_rate: 1e-4 # 1e-4 (3x smaller than before, more stable)
      learning_rate_schedule: linear
      log_interval: 100
      max_grad_norm: 1.0 # ↑ OPTIMIZED: 0.5→1.0 (gradient clipping)
      clip_range: 0.5 # ↑ OPTIMIZED: 0.2→0.5 (2.5x more flexible)
      clip_range_vf: 0.5 # ↑ OPTIMIZED: NEW - Value function clipping
      n_epochs: 10 # ↑ OPTIMIZED: 2→10 (more training passes)
      n_steps: 8760 # 🔴 CRITICAL: 128→8760 (FULL EPISODE, see complete causal chains)
      normalize_advantage: true # ✅ Normalize advantages by batch
      use_sde: true # ↑ OPTIMIZED: NEW - State-Dependent Exploration
      sde_sample_freq: -1 # Resample every step
      normalize_observations: true
      normalize_rewards: true
      reward_scale: 0.5 # AJUSTE: 0.1→0.5 (evita explosión critic_loss)
      clip_reward: 1.0 # ✅ CRITICAL: Clip rewards to [-1, 1]
      clip_obs: 5.0 # ↑ OPTIMIZED: Aggressive obs clipping
      hidden_sizes: [256, 256] # ↑ OPTIMIZED: 64→256 (more capacity)
      multi_objective_weights:
        co2: 0.35   # BALANCED: Prioridad principal
        cost: 0.25  # Optimización de costos
        ev: 0.15    # Satisfacción de EVs
        grid: 0.05  # Estabilidad de red
        solar: 0.20 # Autoconsumo solar
      prefer_citylearn: false
      progress_interval_episodes: 1
      save_final: true
      resume_checkpoints: false
      reward_smooth_lambda: 0.15
    resume_checkpoints: true
    reward_components:
      base_weight: 0.8
      co2_direct_weight: 0.12
      co2_indirect_weight: 0.08
    sac:
      batch_size: 512 # ↑ OPTIMIZED: 256→512 (lotes más grandes)
      buffer_size: 50000 # ↓ OPTIMIZED: 100k→50k (ciclo más rápido)
      checkpoint_freq_steps: 500
      deterministic_eval: true
      device: cuda
      episodes: 3
      ent_coef: auto # ✅ Auto-tuning entropy (adaptive exploration)
      ent_coef_init: 0.2 # ↑ OPTIMIZED: 0.1→0.2 (más exploración inicial para escapar mínimos locales)
      ent_coef_lr: 3e-5 # ↑ OPTIMIZED: 1e-5→3e-5 (ajuste entropy más rápido)
      gamma: 0.995 # ↑ OPTIMIZED: 0.99→0.995 (mayor horizonte temporal)
      tau: 0.02 # ↑ OPTIMIZED: 0.01→0.02 (target network actualiza más rápido)
      gradient_steps: 2 # ↑ OPTIMIZED: 1→2 (más pasos de gradiente por sample)
      learning_rate: 1e-4 # ↑ OPTIMIZED: 5e-5→1e-4 (aprender más rápido)
      learning_starts: 2000 # ↓ OPTIMIZED: 5000→2000 (empezar antes a aprender)
      log_interval: 100
      max_grad_norm: 0.5 # 🔴 CRITICAL FIX: 1.0→0.5 (stricter gradient clipping)
      reward_scale: 0.5 # AJUSTE: 0.1→0.5 (evita explosión critic_loss)
      hidden_sizes: [256, 256] # 🔴 FIX: 512→256 (prevent overfitting)
      use_prioritized_replay: false # 🔴 CRITICAL FIX: Disable PER (causing instability)
      per_alpha: 0.6 # ↑ OPTIMIZED: Prioritization exponent
      per_beta: 0.4 # ↑ OPTIMIZED: Importance sampling
      per_epsilon: 1e-6 # ↑ OPTIMIZED: Min priority epsilon
      normalize_observations: true
      normalize_rewards: true
      clip_reward: 1.0 # ✅ CRITICAL: Clip rewards to [-1, 1]
      clip_obs: 5.0 # ↑ OPTIMIZED: Aggressive obs clipping
      multi_objective_weights:
        co2: 0.35   # BALANCED: Prioridad principal
        cost: 0.25  # Optimización de costos
        ev: 0.15    # Satisfacción de EVs
        grid: 0.05  # Estabilidad de red
        solar: 0.20 # Autoconsumo solar
      prefer_citylearn: false
      progress_interval_episodes: 1
      save_final: true
      train_freq: 1 # ↑ OPTIMIZED: 2→1 (entrenar cada step)
      use_amp: true
      use_sde: true # ↑ OPTIMIZED: false→true (mejor exploración)
  grid:
    carbon_intensity_kg_per_kwh: 0.4521
    tariff_usd_per_kwh: 0.2
paths:
  analyses_dir: analyses
  interim_dir: data/interim
  outputs_dir: outputs
  processed_dir: data/processed
  raw_dir: data/raw
  reports_dir: reports
project:
  seconds_per_time_step: 3600
  seed: 42
  year: 2024
