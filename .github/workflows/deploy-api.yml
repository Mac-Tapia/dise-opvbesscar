name: Deploy Iquitos EV API

on:
    push:
        branches: [main]
        paths:
            - "docker/fastapi-mongo/**"
    pull_request:
        branches: [main]
        paths:
            - "docker/fastapi-mongo/**"
    workflow_dispatch:

env:
    REGISTRY: ghcr.io
    IMAGE_NAME: ${{ github.repository }}/iquitos-ev-api

jobs:
    test:
        name: Test
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.11"

            - name: Install dependencies
              run: |
                  cd docker/fastapi-mongo
                  pip install -r requirements.txt
                  pip install pytest pytest-asyncio httpx

            - name: Run tests
              run: |
                  cd docker/fastapi-mongo
                  python -m pytest tests/ -v || echo "No tests found"

    build:
        name: Build and Push
        runs-on: ubuntu-latest
        needs: test
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        permissions:
            contents: read
            packages: write

        steps:
            - uses: actions/checkout@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to Container Registry
              uses: docker/login-action@v3
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Extract metadata
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
                  tags: |
                      type=sha,prefix=
                      type=ref,event=branch
                      type=raw,value=latest

            - name: Build and push Docker image
              uses: docker/build-push-action@v5
              with:
                  context: docker/fastapi-mongo
                  file: docker/fastapi-mongo/Dockerfile.prod
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    # ==========================================================================
    # DEPLOY JOB - DISABLED BY DEFAULT
    # ==========================================================================
    # To enable deployment, create a new file: .github/workflows/deploy-production.yml
    # with the following content (uncommented):
    #
    # Prerequisites:
    # 1. Create environment "production" in GitHub Settings > Environments
    # 2. Add repository secrets: SERVER_HOST, SERVER_USER, SERVER_SSH_KEY
    #
    # deploy:
    #     name: Deploy to Production
    #     runs-on: ubuntu-latest
    #     needs: build
    #     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    #     environment:
    #         name: production
    #         url: https://iquitos-ev-api.example.com
    #     steps:
    #         - uses: actions/checkout@v4
    #         - name: Deploy to server
    #           uses: appleboy/ssh-action@v1.0.3
    #           with:
    #               host: ${{ secrets.SERVER_HOST }}
    #               username: ${{ secrets.SERVER_USER }}
    #               key: ${{ secrets.SERVER_SSH_KEY }}
    #               script: |
    #                   cd /opt/iquitos-ev-api
    #                   docker compose pull
    #                   docker compose up -d --remove-orphans
    #                   docker system prune -f
    #         - name: Verify deployment
    #           uses: appleboy/ssh-action@v1.0.3
    #           with:
    #               host: ${{ secrets.SERVER_HOST }}
    #               username: ${{ secrets.SERVER_USER }}
    #               key: ${{ secrets.SERVER_SSH_KEY }}
    #               script: |
    #                   sleep 10
    #                   curl -f http://localhost:8000/health || exit 1
    #                   echo "Deployment successful!"
