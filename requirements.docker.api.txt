# requirements.docker.api.txt
# Dependencias adicionales para la imagen Docker de inferencia A2C
# La imagen base pytorch/pytorch ya incluye: torch, numpy, cuda
# Solo instalar lo que falta (~50 MB total)

# RL
stable_baselines3==2.7.1
gymnasium==0.29.1

# Data (numpy ya viene en la base, actualizar si es necesario)
pandas==2.3.3
scipy==1.17.0

# CityLearn v2
citylearn>=2.0.0,<3.0
jsonschema>=4.0.0,<5.0

# FastAPI + async MongoDB
fastapi
uvicorn[standard]
motor
pymongo
pydantic

# Entorno
PyYAML>=6.0
python-dotenv>=1.0
